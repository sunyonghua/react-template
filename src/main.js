import React, { PureComponent } from 'react';
import ReactDOM from 'react-dom';
class Main extends PureComponent {
  render() {
    return <div>react-templates</div>;
  }
}

ReactDOM.render(<Main />, document.getElementById('root'));
